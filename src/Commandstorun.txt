# Make sure no venv is active
deactivate

# Remove old venv if it exists
Remove-Item -Recurse -Force .\venv


# Create a new venv with Python 3.11
py -3.11 -m venv venv

# Activate the venv
.\venv\Scripts\activate


python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt





pip install -r requirements.txt
python -m pip install matplotlib numpy pandas pycoingecko scikit-learn plyer
python -m pip install --force-reinstall scikit-learn
Python run.py


==================
Linux

sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
# Create the venv using specifically 3.11
python3.11 -m venv venv

# Activate the venv
source venv/bin/activate
sudo apt install python3.11 python3.11-venv python3.11-dev
sudo apt install python3.11-tk libxcb-cursor0


# Ensure you are in your venv
source venv/bin/activate

# Upgrade core packaging tools
python3.11 -m pip install --upgrade pip setuptools wheel

# Install everything from your requirements
python3.11 -m pip install -r requirements.txt

# Manually ensure the core stack is present
python3.11 -m pip install matplotlib numpy pandas pycoingecko scikit-learn plyer PyQt5

# Perform the force-reinstall of scikit-learn as you requested
python3.11 -m pip install --force-reinstall scikit-learn